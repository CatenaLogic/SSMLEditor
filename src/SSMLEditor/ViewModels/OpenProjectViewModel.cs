namespace SSMLEditor.ViewModels;

using System;
using Catel.MVVM;

public class OpenProjectViewModel : ViewModelBase
{
    public OpenProjectViewModel(IServiceProvider serviceProvider)
        : base(serviceProvider)
    {
    }
}
